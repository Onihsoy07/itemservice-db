spring:
  profiles:
    active: local

  datasource:
    url: jdbc:h2:tcp://localhost/~/test
    username: sa
    password:

# JdbcTemplate 쿼리 확인
logging:
  level:
    org:
      springframework:
        jdbc: debug